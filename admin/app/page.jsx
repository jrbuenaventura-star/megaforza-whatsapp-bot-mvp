export default function Page(){
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Admin para gestionar productos, clientes, pedidos y capacidades.</p>
      <p>Configura la variable de entorno <code>NEXT_PUBLIC_API_BASE</code> apuntando al backend (por ejemplo: http://localhost:3000/api)</p>
    </div>
  );
}
